/// @description Insert description here
// You can write your code in this editor


if (type == 4) {
	draw_sprite(spr_gutsplatform, currentframe, x, y);
	//draw_text(x, y, mask_index);
} else {
	draw_self();
}